export default async function mergeSort(numbers: Array<number>): Promise<Array<number>> {
    return numbers;
}

function mergeTwoHalves(left: Array<number>, right: Array<number>): Array<number> {
    let mergedArray = [];
    return mergedArray;
}